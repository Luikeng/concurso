import { Settings } from 'lucide-react'
import { ConfigPage } from './ConfigPage'
export default { id: 'config', rotulo: 'Config', descricao: 'Chave de IA, modelo, tema, metas e pomodoro', icone: Settings, path: '/config', ordem: 9, Component: ConfigPage }
