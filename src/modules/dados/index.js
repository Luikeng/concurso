import { DatabaseBackup } from 'lucide-react'
import { DadosPage } from './DadosPage'
export default { id: 'dados', rotulo: 'Dados', descricao: 'Backup/restauração geral, exportar Anki e reset', icone: DatabaseBackup, path: '/dados', ordem: 8, Component: DadosPage }
